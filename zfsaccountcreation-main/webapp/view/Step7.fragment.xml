<core:FragmentDefinition
    xmlns="sap.m"
    xmlns:l="sap.ui.layout"
    xmlns:core="sap.ui.core"
    xmlns:layout="sap.ui.layout"
    xmlns:form="sap.ui.layout.form"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns:u="sap.ui.unified"
    xmlns:f="sap.f"
    controllerName="zfsaccountcreation.controller.MainWizard"
>
    

    <f:DynamicPage
        id="dynamicPageId"
        stickySubheaderProvider="iconTabBar"
        class="sapUiNoContentPadding"
        toggleHeaderOnTitleClick="{/titleClickable}"
    >
        <!-- DynamicPage Title -->
        <f:title>
            <f:DynamicPageTitle>
                <f:heading>
                    <Title text="Final Values" />
                </f:heading>
                <f:breadcrumbs>
                    <core:Fragment
                        fragmentName="zfsaccountcreation.view.StepB"
                        type="XML"
                    />
                </f:breadcrumbs>
                <f:expandedContent>
                    <Label
                        text="{i18n>Proposed}" 
                        required="true"
                    />
                    <Input
                        editable="{StepModel>/ApprovalVisible}"
                        value="{StepModel>/SelectedLevel6}"
                    />
                </f:expandedContent>
                <f:snappedContent>
                    <Label
                        text="{i18n>Proposed}"
                        required="true"
                    />
                    <Input
                        editable="{StepModel>/ApprovalVisible}"
                        value="{StepModel>/SelectedLevel6}"
                    />
                </f:snappedContent>
                <f:snappedTitleOnMobile />
                <f:content />
            </f:DynamicPageTitle>
        </f:title>
        <!-- DynamicPage Header -->
        <f:header>
            <f:DynamicPageHeader pinnable="false">
                <layout:HorizontalLayout allowWrapping="false">
                    <layout:VerticalLayout  />
                    <Table items="{StepModel>/Level6items}">
                        <infoToolbar>
                            <OverflowToolbar>
                                <Label text="Below are alredy Existing Values" />
                            </OverflowToolbar>
                        </infoToolbar>
                        <columns>
                            <Column>
                                <Text text="Existing Values" />
                            </Column>
                            <Column>
                                <Text text="Description" />
                            </Column>
                        </columns>
                        <items>
                            <ColumnListItem>
                                <cells>
                                    <Text text="{StepModel>Level06}" />
                                    <Text text="{StepModel>Description}" />
                                </cells>
                            </ColumnListItem>
                        </items>
                    </Table>
                    <layout:VerticalLayout />
                </layout:HorizontalLayout>
            </f:DynamicPageHeader>
        </f:header>

        <f:content>
           
        </f:content>
    </f:DynamicPage>

   
</core:FragmentDefinition>
